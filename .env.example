# ECE Factory — environment template. Copy to .env and fill locally. Never commit real secrets.
# Values are documented in docs/DEPLOYMENT.md (to be completed in Phase 2).

# Deployment environment: local | staging | production
NODE_ENV=local

# PostgreSQL (local / sovereign — no foreign cloud)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DBNAME
